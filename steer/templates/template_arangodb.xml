<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'arangodb' (imagen arangodb:3.12.4) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_arangodb" model="service.template">
        <field name="name">ArangoDB</field>
        <field name="code">arangodb</field>
        <field name="service_code">arangodb</field>
        <field name="version">latest</field>
        <field name="description">ArangoDB is a native multi-model database that unifies documents, graphs, and key-value data behind a single query language.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">ArangoDB is a native multi-model database that unifies documents, graphs, and key-value data behind a single query language.</p>
  <p>It stores JSON documents, graph relationships, and key-value pairs in one engine, all queried through AQL (ArangoDB Query Language), an expressive SQL-inspired language. Written in C++ for performance at scale, it lets teams model connected data and run graph traversals, joins, and full-text search without stitching together separate databases. It is aimed at developers and platform teams who need flexible data modeling in a single backend.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Native multi-model engine: documents, graphs, and key-value in one core</li>
    <li>AQL, a unified SQL-inspired query language across all data models</li>
    <li>Native graph store with multi-hop traversals, shortest-path, and pattern matching</li>
    <li>ArangoSearch: integrated full-text search, ranking, and information retrieval</li>
    <li>Horizontal scaling with clustering, sharding, and replication</li>
    <li>JavaScript-based Foxx microservices and a built-in web admin interface</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Applications with highly connected or heterogeneous data, fraud detection, recommendation engines, knowledge graphs, and any team that wants document, graph, and search capabilities consolidated in a single self-hosted backend.</p>
</div>]]></field>
        <field name="category">database</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/arangodb.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_database'), ref('agz_infra_suite.tag_search'), ref('agz_infra_suite.tag_analytics')])]"/>
        <field name="website_url">https://www.arangodb.com/</field>
        <field name="documentation_url">https://www.arangodb.com/docs/</field>
        <field name="source_url">https://github.com/arangodb/arangodb</field>
        <field name="docker_compose_template"><![CDATA[services:
  arangodb:
    image: arangodb:3.12.4
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:8529'
    environment:
    - ARANGO_ROOT_PASSWORD={{ arango_password }}
    volumes:
    - ./data:/var/lib/arangodb3
    container_name: '{{ INSTANCE_CODE }}'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_arangodb_port_http" model="service.template.port">
        <field name="template_id" ref="template_arangodb"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8529</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_arangodb_var_arango_password" model="service.template.variable">
        <field name="template_id" ref="template_arangodb"/>
        <field name="name">arango_password</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_arangodb_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_arangodb"/>
        <field name="name">data</field>
        <field name="local_path">./data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
