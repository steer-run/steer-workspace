<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'kutt' (imagen kutt/kutt:main) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_kutt" model="service.template">
        <field name="name">Kutt</field>
        <field name="code">kutt</field>
        <field name="service_code">kutt</field>
        <field name="version">latest</field>
        <field name="description">Kutt is a free, modern, self-hosted URL shortener with custom domains, link analytics, and a clean management dashboard.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Kutt is a free, modern, self-hosted URL shortener with custom domains, link analytics, and a clean management dashboard.</p>
  <p>Kutt lets teams and individuals run their own branded link-shortening service instead of relying on third-party providers. It runs on Node.js with a choice of SQLite, PostgreSQL, or MySQL/MariaDB for storage and optional Redis caching, and ships an official Docker image for zero-config deployment. Links can be customized, password-protected, set to expire, and tracked with private statistics.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Custom short links with passwords, descriptions, and expiration dates</li>
    <li>Multiple custom domains with HTTPS support</li>
    <li>Private per-link statistics and an admin dashboard for users and links</li>
    <li>RESTful API plus OpenID Connect (OIDC) authentication</li>
    <li>Pluggable storage (SQLite, PostgreSQL, MySQL/MariaDB) with optional Redis cache</li>
    <li>Built-in rate limiting and a themeable UI</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams, agencies, and individuals who want a self-hosted, privacy-respecting alternative to commercial link shorteners, with branded domains and full control over their data.</p>
</div>]]></field>
        <field name="category">productivity</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/kutt.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_productivity'), ref('agz_infra_suite.tag_analytics'), ref('agz_infra_suite.tag_identity'), ref('agz_infra_suite.tag_url_shortener')])]"/>
        <field name="website_url">https://kutt.it</field>
        <field name="documentation_url">https://github.com/thedevs-network/kutt#kuttit</field>
        <field name="source_url">https://github.com/thedevs-network/kutt</field>
        <field name="docker_compose_template"><![CDATA[services:
  kutt:
    image: kutt/kutt:main
    volumes:
    - ./kutt_db_data:/var/lib/kutt
    - ./kutt/custom:/kutt/custom
    environment:
      DB_FILENAME: /var/lib/kutt/data.sqlite
      JWT_SECRET: ${JWT_SECRET}
      DEFAULT_DOMAIN: {{ instance.full_domain }}
      TRUST_PROXY: ${TRUST_PROXY}
      DISALLOW_ANONYMOUS_LINKS: ${DISALLOW_ANONYMOUS_LINKS}
      CUSTOM_DOMAIN_USE_HTTPS: ${CUSTOM_DOMAIN_USE_HTTPS}
      MAIL_ENABLED: ${MAIL_ENABLED}
      MAIL_HOST: ${MAIL_HOST}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_SECURE: ${MAIL_SECURE}
      MAIL_USER: ${MAIL_USER}
      MAIL_FROM: ${MAIL_FROM}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      CONTACT_EMAIL: ${CONTACT_EMAIL}
    container_name: '{{ INSTANCE_CODE }}'
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:3000'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_kutt_port_http" model="service.template.port">
        <field name="template_id" ref="template_kutt"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">3000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_kutt_var_jwt_secret" model="service.template.variable">
        <field name="template_id" ref="template_kutt"/>
        <field name="name">JWT_SECRET</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_kutt_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_kutt"/>
        <field name="name">kutt_db_data</field>
        <field name="local_path">./kutt_db_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_kutt_var_mail_password" model="service.template.variable">
        <field name="template_id" ref="template_kutt"/>
        <field name="name">MAIL_PASSWORD</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">20</field>
    </record>
</odoo>
