<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'gitea' (imagen docker.gitea.com/gitea:1.24.4) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_gitea_sqlite" model="service.template">
        <field name="name">Gitea (SQLite)</field>
        <field name="code">gitea_sqlite</field>
        <field name="service_code">gitea_sqlite</field>
        <field name="version">1.24.4</field>
        <field name="description">Gitea is a lightweight, all-in-one self-hosted Git service that runs as a single Go binary and stores everything in a built-in SQLite database for the simplest possible one-container setup.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Gitea is a lightweight, all-in-one self-hosted Git service that runs as a single Go binary and stores everything in a built-in SQLite database for the simplest possible one-container setup.</p>
  <p>Gitea delivers Git hosting, code review, issue tracking, wikis, a package registry and GitHub Actions-compatible CI/CD with a tiny footprint. The SQLite variant needs no separate database server: the database file is created automatically on first run, making it ideal for home labs, single-user setups and small private teams. It starts in seconds and runs comfortably on a 1 GB RAM VPS.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Full Git forge: repositories, pull requests, code review and team collaboration</li>
    <li>Built-in SQLite backend with zero external database to install or manage</li>
    <li>Gitea Actions: integrated CI/CD compatible with GitHub Actions workflows</li>
    <li>Package registry supporting 20+ formats (Container, npm, PyPI, Maven, NuGet, Helm and more)</li>
    <li>Issue tracking, wikis, milestones and organization/team permissions</li>
    <li>Single self-contained binary; broad OS and CPU architecture support</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Home labs, individual developers and small private teams that want a fast, low-resource Git server without the overhead of a standalone database. For large or public multi-user deployments, a server-based database (PostgreSQL or MySQL/MariaDB) is recommended instead.</p>
</div>]]></field>
        <field name="category">dev_tools</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/gitea_sqlite.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_git'), ref('agz_infra_suite.tag_dev_tools'), ref('agz_infra_suite.tag_ci_cd'), ref('agz_infra_suite.tag_collaboration')])]"/>
        <field name="website_url">https://gitea.io/</field>
        <field name="documentation_url">https://docs.gitea.com/</field>
        <field name="source_url">https://github.com/go-gitea/gitea</field>
        <field name="docker_compose_template"><![CDATA[services:
  gitea:
    image: docker.gitea.com/gitea:1.24.4
    restart: unless-stopped
    environment:
    - USER_UID=1000
    - USER_GID=1000
    volumes:
    - ./gitea_data:/data
    expose:
    - '3000'
    - '22'
    healthcheck:
      test:
      - CMD
      - wget
      - -q
      - --spider
      - http://localhost:3000/
      interval: 15s
      timeout: 5s
      retries: 10
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:3000'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_gitea_sqlite_port_http" model="service.template.port">
        <field name="template_id" ref="template_gitea_sqlite"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">3000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_gitea_sqlite_var_user_uid" model="service.template.variable">
        <field name="template_id" ref="template_gitea_sqlite"/>
        <field name="name">user_uid</field>
        <field name="var_type">string</field>
        <field name="default_value">1000</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_gitea_sqlite_var_user_gid" model="service.template.variable">
        <field name="template_id" ref="template_gitea_sqlite"/>
        <field name="name">user_gid</field>
        <field name="var_type">string</field>
        <field name="default_value">1000</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_gitea_sqlite_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_gitea_sqlite"/>
        <field name="name">gitea-data</field>
        <field name="local_path">./gitea_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
