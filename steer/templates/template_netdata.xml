<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'netdata' (imagen netdata/netdata:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_netdata" model="service.template">
        <field name="name">Netdata</field>
        <field name="code">netdata</field>
        <field name="service_code">netdata</field>
        <field name="version">latest</field>
        <field name="description">Netdata is a real-time, zero-configuration observability platform that turns any server, container, or device into a high-resolution monitoring node.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Netdata is a real-time, zero-configuration observability platform that turns any server, container, or device into a high-resolution monitoring node.</p>
  <p>It collects per-second metrics from the operating system, hardware, containers, and hundreds of applications, then visualizes them on interactive dashboards the moment the agent starts. Built-in machine learning continuously profiles each metric to surface anomalies, while a distributed, edge-native architecture lets thousands of nodes stream data to parent instances without a central database bottleneck. It targets sysadmins, SREs, and DevOps teams that need granular, low-overhead insight without assembling a monitoring stack by hand.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Per-second metrics with auto-discovery and out-of-the-box dashboards, no manual configuration required</li>
    <li>Hundreds of collectors for systems, containers, databases, web servers, and applications</li>
    <li>Unsupervised machine-learning anomaly detection running locally on each node</li>
    <li>Health engine with hundreds of preconfigured alarms and multi-channel notifications</li>
    <li>Edge-native streaming and replication to parent nodes for centralized views and longer retention</li>
    <li>Lightweight footprint with an efficient time-series database optimized for low CPU and RAM use</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams that want immediate, granular visibility into individual servers, fleets, and containerized workloads, from a single self-hosted host to large distributed infrastructures, without the operational cost of building and tuning a traditional metrics pipeline.</p>
</div>]]></field>
        <field name="category">monitoring</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/netdata.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_monitoring'), ref('agz_infra_suite.tag_analytics'), ref('agz_infra_suite.tag_logging'), ref('agz_infra_suite.tag_dev_tools')])]"/>
        <field name="website_url">https://www.netdata.cloud/</field>
        <field name="documentation_url">https://learn.netdata.cloud/</field>
        <field name="source_url">https://github.com/netdata/netdata</field>
        <field name="docker_compose_template"><![CDATA[services:
  netdata:
    image: netdata/netdata:latest
    restart: unless-stopped
    cap_add:
    - SYS_PTRACE
    - SYS_ADMIN
    security_opt:
    - apparmor:unconfined
    volumes:
    - ./netdata_config:/etc/netdata
    - ./netdata_lib:/var/lib/netdata
    - ./netdata_cache:/var/cache/netdata
    - /etc/passwd:/host/etc/passwd:ro
    - /etc/group:/host/etc/group:ro
    - /proc:/host/proc:ro
    - /sys:/host/sys:ro
    - /etc/os-release:/host/etc/os-release:ro
    - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
    - NETDATA_CLAIM_TOKEN=${NETDATA_CLAIM_TOKEN:-}
    - NETDATA_CLAIM_URL=${NETDATA_CLAIM_URL:-}
    - NETDATA_CLAIM_ROOMS=${NETDATA_CLAIM_ROOMS:-}
    ports:
    - '{{ PORT_HTTP }}:19999'
    container_name: '{{ INSTANCE_CODE }}'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_netdata_port_http" model="service.template.port">
        <field name="template_id" ref="template_netdata"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">19999</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_netdata_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_netdata"/>
        <field name="name">netdata-config</field>
        <field name="local_path">./netdata_config</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_netdata_vol_1" model="service.template.volume">
        <field name="template_id" ref="template_netdata"/>
        <field name="name">netdata-lib</field>
        <field name="local_path">./netdata_lib</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_netdata_vol_2" model="service.template.volume">
        <field name="template_id" ref="template_netdata"/>
        <field name="name">netdata-cache</field>
        <field name="local_path">./netdata_cache</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">30</field>
    </record>
</odoo>
