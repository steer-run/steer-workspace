<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="template_documenso" model="service.template">
        <field name="name">Documenso</field>
        <field name="code">documenso</field>
        <field name="service_code">documenso</field>
        <field name="version">v1.5.6</field>
        <field name="description">Documenso is an open-source electronic signature platform — a self-hostable alternative to DocuSign for signing documents with legal validity.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Documenso is an open-source electronic signature platform — a self-hostable alternative to DocuSign for signing documents with legal validity.</p>
  <p>It lets teams upload, send, and sign PDF documents through a modern web interface, supporting reusable templates, multi-signer workflows, and tamper-evident audit trails. Built on a TypeScript stack (Next.js, Prisma, PostgreSQL), it signs documents cryptographically with PKCS#12 certificates so signatures hold up under eIDAS and ESIGN frameworks. It targets developers and organizations that want full control over their signing infrastructure rather than relying on a proprietary SaaS.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Cryptographic PDF signing with PKCS#12 certificates (eIDAS/ESIGN aligned)</li>
    <li>Reusable document templates and multi-signer ordering workflows</li>
    <li>Tamper-evident audit trail capturing signer identity, IP, and timestamps</li>
    <li>REST API and webhooks for automation and integration</li>
    <li>Team management, custom branding, and single sign-on (SSO)</li>
    <li>Self-hosting via Docker, Docker Compose, and Kubernetes</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Businesses, agencies, and developers that need legally valid digital signatures for contracts, NDAs, and forms while keeping documents on infrastructure they control. A strong fit for regulated workflows where cryptographic signature validity matters.</p>
</div>]]></field>
        <field name="category">business</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">True</field>
        <field name="database_type">postgres</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">custom</field>
        <field name="health_check_command">pg_isready -U $POSTGRES_USER</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/documenso.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_security'), ref('agz_infra_suite.tag_productivity'), ref('agz_infra_suite.tag_collaboration'), ref('agz_infra_suite.tag_dev_tools')])]"/>
        <field name="website_url">https://documenso.com/</field>
        <field name="documentation_url">https://documenso.com/docs</field>
        <field name="source_url">https://github.com/documenso/documenso</field>
        <field name="docker_compose_template"><![CDATA[services:
  postgres:
    image: postgres:16
    volumes:
    - ./documenso_data:/var/lib/postgresql/data
    environment:
    - POSTGRES_USER=documenso
    - POSTGRES_PASSWORD=password
    - POSTGRES_DB=documenso
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U documenso
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    container_name: '{{ INSTANCE_CODE }}_postgres'
    restart: unless-stopped
    networks:
    - internal
  documenso:
    image: documenso/documenso:v1.12.10
    depends_on:
      postgres:
        condition: service_healthy
    environment:
    - PORT=${DOCUMENSO_PORT}
    - NEXTAUTH_URL=http://{{ instance.full_domain }}
    - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
    - NEXT_PRIVATE_ENCRYPTION_KEY=${NEXT_PRIVATE_ENCRYPTION_KEY}
    - NEXT_PRIVATE_ENCRYPTION_SECONDARY_KEY=${NEXT_PRIVATE_ENCRYPTION_SECONDARY_KEY}
    - NEXT_PUBLIC_WEBAPP_URL=http://{{ instance.full_domain }}
    - NEXT_PRIVATE_DATABASE_URL=postgres://documenso:password@postgres:5432/documenso
    - NEXT_PRIVATE_DIRECT_DATABASE_URL=postgres://documenso:password@postgres:5432/documenso
    - NEXT_PUBLIC_UPLOAD_TRANSPORT=database
    - NEXT_PRIVATE_SMTP_TRANSPORT=smtp-auth
    - NEXT_PRIVATE_SIGNING_TRANSPORT=local
    - NEXT_PRIVATE_SIGNING_LOCAL_FILE_PATH=/app/certs/cert.p12
    - NEXT_PRIVATE_SIGNING_LOCAL_FILE_PASSPHRASE=${NEXT_PRIVATE_SIGNING_LOCAL_FILE_PASSPHRASE}
    - CERT_VALID_DAYS=${CERT_VALID_DAYS:-365}
    - CERT_INFO_COUNTRY_NAME=${CERT_INFO_COUNTRY_NAME:-US}
    - CERT_INFO_STATE_OR_PROVIDENCE=${CERT_INFO_STATE_OR_PROVIDENCE:-State}
    - CERT_INFO_LOCALITY_NAME=${CERT_INFO_LOCALITY_NAME:-City}
    - CERT_INFO_ORGANIZATION_NAME=${CERT_INFO_ORGANIZATION_NAME:-Organization}
    - CERT_INFO_ORGANIZATIONAL_UNIT=${CERT_INFO_ORGANIZATIONAL_UNIT:-IT Department}
    - CERT_INFO_EMAIL=${CERT_INFO_EMAIL:-admin@example.com}
    - DOCUMENSO_HOST={{ instance.full_domain }}
    ports:
    - '{{ PORT_HTTP }}:3000'
    entrypoint:
    - /bin/sh
    - -c
    - "CERT_PASSPHRASE=\"$${NEXT_PRIVATE_SIGNING_LOCAL_FILE_PASSPHRASE}\"\n\n# Save original working directory\n\
      ORIGINAL_DIR=\"$$(pwd)\"\n\n# Find openssl binary (should be available in v1.12.10+)\nOPENSSL_CMD=\"\
      $$(which openssl 2>/dev/null || command -v openssl 2>/dev/null || echo '/usr/bin/openssl')\"\n\n\
      # Verify openssl is available\nif ! $$OPENSSL_CMD version >/dev/null 2>&1; then\n  echo \"Error:\
      \ OpenSSL not found. Please use Documenso image v1.12.10 or later.\"\n  exit 1\nfi\n\n# Create certificate\
      \ directory - use /app/certs (writable by user 1001)\nCERT_DIR=\"/app/certs\"\nmkdir -p \"$$CERT_DIR\"\
      \ || {\n  # Fallback to tmp if app directory not writable\n  CERT_DIR=\"/tmp/certs\"\n  mkdir -p\
      \ \"$$CERT_DIR\"\n  echo \"Warning: Using fallback directory: $$CERT_DIR\"\n}\n\ntouch /tmp/cert_info_path\n\
      cat <<EOF > /tmp/cert_info_path\n[ req ]\ndistinguished_name = req_distinguished_name\nprompt =\
      \ no\n[ req_distinguished_name ]\nC            = $${CERT_INFO_COUNTRY_NAME}\nST           = $${CERT_INFO_STATE_OR_PROVIDENCE}\n\
      L            = $${CERT_INFO_LOCALITY_NAME}\nO            = $${CERT_INFO_ORGANIZATION_NAME}\nOU \
      \          = $${CERT_INFO_ORGANIZATIONAL_UNIT}\nCN           = $${DOCUMENSO_HOST}\nemailAddress\
      \ = $${CERT_INFO_EMAIL}\nEOF\n\ncd \"$$CERT_DIR\"\n\n$$OPENSSL_CMD genrsa -out private.key 2048\n\
      \n$$OPENSSL_CMD req \\\n  -new \\\n  -x509 \\\n  -key private.key \\\n  -out certificate.crt \\\n\
      \  -days $${CERT_VALID_DAYS} \\\n  -config /tmp/cert_info_path\n\n$$OPENSSL_CMD pkcs12 \\\n  -export\
      \ \\\n  -out cert.p12 \\\n  -inkey private.key \\\n  -in certificate.crt \\\n  -legacy \\\n  -passout\
      \ pass:\"$$CERT_PASSPHRASE\"\n\n# Set permissions (may fail if not root, but will work in Coolify)\n\
      chown 1001:1001 cert.p12 private.key certificate.crt 2>/dev/null || true\nchmod 400 cert.p12 private.key\
      \ certificate.crt\n\n# Update environment variable if directory changed\nif [ \"$$CERT_DIR\" !=\
      \ \"/app/certs\" ]; then\n  export NEXT_PRIVATE_SIGNING_LOCAL_FILE_PATH=\"$$CERT_DIR/cert.p12\"\n\
      fi\n\n# Return to original directory before starting application\ncd \"$$ORIGINAL_DIR\"\n\n./start.sh\n"
    container_name: '{{ INSTANCE_CODE }}'
    restart: unless-stopped
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
        <field name="backup_script"><![CDATA[#!/bin/bash
set -euo pipefail
cd "$SERVICE_PATH"
# Dump consistente de PostgreSQL (servicio en marcha) → $BACKUP_DATA_DIR
docker compose exec -T postgres sh -c 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" --no-owner -Fc' \
  > "$BACKUP_DATA_DIR/db.dump"
echo "pg_dump OK ($(wc -c < "$BACKUP_DATA_DIR/db.dump") bytes)"
]]></field>
        <field name="restore_script"><![CDATA[#!/bin/bash
set -euo pipefail
cd "$SERVICE_PATH"
# Levanta solo la DB, espera readiness y restaura
docker compose up -d postgres
for i in $(seq 1 30); do
  docker compose exec -T postgres sh -c 'pg_isready -U "$POSTGRES_USER"' && break
  sleep 2
done
docker compose exec -T postgres sh -c 'pg_restore -U "$POSTGRES_USER" -d "$POSTGRES_DB" --clean --if-exists --no-owner' \
  < "$RESTORE_DATA_DIR/db.dump"
# Verificación: la DB no quedó vacía (sh -c con dobles comillas; $VAR del contenedor con \$)
N=$(docker compose exec -T postgres sh -c "psql -U \$POSTGRES_USER -d \$POSTGRES_DB -tAc \"SELECT count(*) FROM information_schema.tables WHERE table_schema='public'\"")
echo "tablas restauradas: $N"
test "$N" -gt 0
]]></field>
    </record>
    <record id="template_documenso_port_http" model="service.template.port">
        <field name="template_id" ref="template_documenso"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">3000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_documenso_var_nextauth_secret" model="service.template.variable">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">NEXTAUTH_SECRET</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_documenso_var_encryption_key" model="service.template.variable">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">NEXT_PRIVATE_ENCRYPTION_KEY</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_documenso_var_secondary_encryption_key" model="service.template.variable">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">secondary_encryption_key</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">30</field>
    </record>
    <record id="template_documenso_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">documenso-data</field>
        <field name="local_path">./documenso_data</field>
        <field name="owner">999:999</field>
        <field name="exclude_from_backup">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_documenso_var_next_private_encryption_secondary_key" model="service.template.variable">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">NEXT_PRIVATE_ENCRYPTION_SECONDARY_KEY</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">40</field>
    </record>
    <record id="template_documenso_var_next_private_signing_local_file_passphrase" model="service.template.variable">
        <field name="template_id" ref="template_documenso"/>
        <field name="name">NEXT_PRIVATE_SIGNING_LOCAL_FILE_PASSPHRASE</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">50</field>
    </record>
</odoo>
