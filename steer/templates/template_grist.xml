<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'grist' (imagen gristlabs/grist:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_grist" model="service.template">
        <field name="name">Grist</field>
        <field name="code">grist</field>
        <field name="service_code">grist</field>
        <field name="version">latest</field>
        <field name="description">Grist is a relational spreadsheet that merges the ease of a spreadsheet with the structure and power of a real database.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Grist is a relational spreadsheet that merges the ease of a spreadsheet with the structure and power of a real database.</p>
  <p>It lets teams model data with typed columns, link tables by reference, and compute values with full Python formulas instead of opaque cell syntax. Documents are self-contained SQLite files with no vendor lock-in, while granular access rules and SSO make it suitable for sensitive, multi-user deployments. It is the open-source alternative to tools like Airtable, with self-hosting as a first-class option.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Typed columns, linked reference tables and relational data modeling</li>
    <li>Python formulas with automatic recalculation across the document</li>
    <li>Granular row- and column-level access control rules with role testing</li>
    <li>Dashboards with charts, summary tables, forms and custom widgets</li>
    <li>REST API and webhooks for integrations and automation</li>
    <li>Portable SQLite documents, CSV/XLSX export and SSO (OIDC, SAML, OAuth, SCIM)</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams and organizations that have outgrown plain spreadsheets and need structured, governed data: internal tooling, CRMs and trackers, regulated or government workloads requiring data sovereignty, and no-code app builders who want an Airtable-style experience under their own control.</p>
</div>]]></field>
        <field name="category">database</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/grist.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_database'), ref('agz_infra_suite.tag_no_code'), ref('agz_infra_suite.tag_productivity'), ref('agz_infra_suite.tag_collaboration'), ref('agz_infra_suite.tag_analytics')])]"/>
        <field name="website_url">https://www.getgrist.com/</field>
        <field name="documentation_url">https://support.getgrist.com/self-managed/</field>
        <field name="source_url">https://github.com/gristlabs/grist-core</field>
        <field name="docker_compose_template"><![CDATA[services:
  grist:
    image: gristlabs/grist:latest
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:8484'
    volumes:
    - ./grist_data:/persist
    environment:
      GRIST_DEFAULT_EMAIL: ${GRIST_DEFAULT_EMAIL:-}
      GRIST_SESSION_SECRET: ${GRIST_SESSION_SECRET:-}
      GRIST_SINGLE_ORG: ${GRIST_SINGLE_ORG:-}
      GRIST_HOME_INSTANCE: ${GRIST_HOME_INSTANCE:-}
    container_name: '{{ INSTANCE_CODE }}'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_grist_port_http" model="service.template.port">
        <field name="template_id" ref="template_grist"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8484</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_grist_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_grist"/>
        <field name="name">grist_data</field>
        <field name="local_path">./grist_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
