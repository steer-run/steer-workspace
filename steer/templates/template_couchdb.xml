<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'couchdb' (imagen couchdb:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_couchdb" model="service.template">
        <field name="name">CouchDB</field>
        <field name="code">couchdb</field>
        <field name="service_code">couchdb</field>
        <field name="version">latest</field>
        <field name="description">Apache CouchDB is a document-oriented NoSQL database built for reliable replication and offline-first synchronization through an intuitive HTTP/JSON API.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Apache CouchDB is a document-oriented NoSQL database built for reliable replication and offline-first synchronization through an intuitive HTTP/JSON API.</p>
  <p>It stores schema-free JSON documents and exposes every operation over a RESTful HTTP interface, so any HTTP client can read, write and query data without a driver. Its standout capability is multi-primary replication: nodes and devices can sync bidirectionally, work offline, and reconcile changes later, making it ideal for distributed and edge scenarios. Backed by the Apache Software Foundation and written in Erlang for fault tolerance, it pairs naturally with PouchDB for offline-first web and mobile apps.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Schema-free JSON document storage with a RESTful HTTP/JSON API</li>
    <li>Multi-primary (master-master) bidirectional replication and conflict handling</li>
    <li>Offline-first synchronization via the Couch Replication Protocol (PouchDB-compatible)</li>
    <li>Map/reduce views and Mango (declarative JSON) queries for indexing and filtering</li>
    <li>Clustering and horizontal scaling with sharding across nodes</li>
    <li>Built-in admin web UI (Fauxton), changes feeds and full-text search (Nouveau/Lucene)</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams building distributed, offline-first or edge applications that need dependable bidirectional sync between servers, browsers and mobile devices, plus anyone wanting a battle-tested, self-hosted NoSQL store with a simple HTTP API.</p>
</div>]]></field>
        <field name="category">database</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/couchdb.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_database')])]"/>
        <field name="website_url">https://couchdb.apache.org/</field>
        <field name="documentation_url">https://docs.couchdb.org/en/stable/</field>
        <field name="source_url">https://github.com/apache/couchdb</field>
        <field name="docker_compose_template"><![CDATA[services:
  couchdb:
    image: couchdb:latest
    ports:
    - '{{ PORT_HTTP }}:5984'
    volumes:
    - ./couchdb_data:/opt/couchdb/data
    environment:
    - COUCHDB_USER=${COUCHDB_USER}
    - COUCHDB_PASSWORD=${COUCHDB_PASSWORD}
    restart: unless-stopped
    container_name: '{{ INSTANCE_CODE }}'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_couchdb_port_http" model="service.template.port">
        <field name="template_id" ref="template_couchdb"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">5984</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_couchdb_var_username" model="service.template.variable">
        <field name="template_id" ref="template_couchdb"/>
        <field name="name">username</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_couchdb_var_password" model="service.template.variable">
        <field name="template_id" ref="template_couchdb"/>
        <field name="name">COUCHDB_PASSWORD</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_couchdb_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_couchdb"/>
        <field name="name">couchdb-data</field>
        <field name="local_path">./couchdb_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
