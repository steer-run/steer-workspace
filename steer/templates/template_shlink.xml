<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Multi-servicio (además de 'shlink-web': shlink): revisar backup/red/init.
           - Owner UID:GID de 'shlink' (imagen shlinkio/shlink:stable) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_shlink" model="service.template">
        <field name="name">Shlink</field>
        <field name="code">shlink</field>
        <field name="service_code">shlink</field>
        <field name="version">stable</field>
        <field name="description">Shlink is a self-hosted URL shortener that serves short links under your own domains while keeping full ownership of your data and analytics.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Shlink is a self-hosted URL shortener that serves short links under your own domains while keeping full ownership of your data and analytics.</p>
  <p>Built in PHP, Shlink exposes a complete REST API and command-line interface to create, manage and track short URLs across multiple custom domains. It records anonymized visit statistics with geolocation, supports custom slugs and tags, and stores data in your choice of database. A separate React web client and dashboard provide a friendly UI on top of the API.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>REST API and CLI for full programmatic and terminal management</li>
    <li>Multiple custom domains, human-friendly custom slugs and link tagging</li>
    <li>Detailed visit analytics with geolocation (MaxMind GeoLite2) and orphan-visit tracking</li>
    <li>Pluggable database backends: MySQL, MariaDB, PostgreSQL, Microsoft SQL Server or SQLite</li>
    <li>API key management with granular permissions and roles</li>
    <li>Official Docker image with health checks and automatic migrations</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Developers, teams and organizations that want a privacy-respecting, brandable URL shortener under their own domain, with an API-first design for integration into existing apps and marketing workflows.</p>
</div>]]></field>
        <field name="category">networking</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/shlink.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_networking'), ref('agz_infra_suite.tag_analytics'), ref('agz_infra_suite.tag_dev_tools'), ref('agz_infra_suite.tag_privacy'), ref('agz_infra_suite.tag_url_shortener')])]"/>
        <field name="website_url">https://shlink.io</field>
        <field name="documentation_url">https://shlink.io/documentation</field>
        <field name="source_url">https://github.com/shlinkio/shlink</field>
        <field name="docker_compose_template"><![CDATA[services:
  shlink:
    image: shlinkio/shlink:stable
    environment:
    - INITIAL_API_KEY={{ initial_api_key }}
    - DEFAULT_DOMAIN={{ default_domain }}
    - IS_HTTPS_ENABLED=false
    volumes:
    - ./shlink_data:/etc/shlink/data
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://127.0.0.1:8080/rest/v3/health
      interval: 30s
      timeout: 10s
      retries: 3
    container_name: '{{ INSTANCE_CODE }}_shlink'
    restart: unless-stopped
    networks:
    - internal
  shlink-web:
    image: shlinkio/shlink-web-client
    environment:
    - SHLINK_SERVER_API_KEY={{ initial_api_key }}
    - SHLINK_SERVER_URL=http://{{ default_domain }}
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://127.0.0.1:8080
      interval: 30s
      timeout: 10s
      retries: 3
    container_name: '{{ INSTANCE_CODE }}'
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:8080'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_shlink_port_http" model="service.template.port">
        <field name="template_id" ref="template_shlink"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8080</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_shlink_var_initial_api_key" model="service.template.variable">
        <field name="template_id" ref="template_shlink"/>
        <field name="name">initial_api_key</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_shlink_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_shlink"/>
        <field name="name">shlink-data</field>
        <field name="local_path">./shlink_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
