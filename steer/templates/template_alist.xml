<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'alist' (imagen xhofe/alist:v3.55.0) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_alist" model="service.template">
        <field name="name">AList</field>
        <field name="code">alist</field>
        <field name="service_code">alist</field>
        <field name="version">v3.55.0</field>
        <field name="description">AList is a self-hosted file list and WebDAV gateway that unifies dozens of local and cloud storage providers behind a single web interface.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">AList is a self-hosted file list and WebDAV gateway that unifies dozens of local and cloud storage providers behind a single web interface.</p>
  <p>Built with Go (Gin) and SolidJS, AList aggregates backends such as local disk, OneDrive, Google Drive, S3-compatible object storage and many regional cloud drives, then exposes them through a fast browsable UI and a standard WebDAV endpoint. It adds in-browser preview for video, audio, images, PDF, Office and code files, plus per-path permissions, sharing and optional client-side encryption. It is a popular choice for turning scattered storage accounts into one cohesive, mountable file portal.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Aggregates many local and cloud storage backends behind one interface</li>
    <li>Standard WebDAV server, mountable from Windows, macOS, Linux and media players</li>
    <li>In-browser preview for video, audio, images, PDF, Office and code</li>
    <li>Path-based access control, public sharing links and guest access</li>
    <li>Optional client-side encryption (Crypt) so providers store only ciphertext</li>
    <li>Lightweight single binary with first-class Docker deployment</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Individuals and teams who want a single browsable, WebDAV-mountable front end over multiple cloud and local storage accounts for downloading, streaming and lightweight file sharing. Given its history and broad external reach, run it behind authentication and review telemetry and external-link settings before exposing it.</p>
</div>]]></field>
        <field name="category">storage</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/alist.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_storage'), ref('agz_infra_suite.tag_file_sharing'), ref('agz_infra_suite.tag_media')])]"/>
        <field name="website_url">https://alistgo.com/</field>
        <field name="documentation_url">https://alistgo.com/guide/install/docker.html</field>
        <field name="source_url">https://github.com/AlistGo/alist</field>
        <field name="docker_compose_template"><![CDATA[services:
  alist:
    image: xhofe/alist:v3.55.0
    volumes:
    - ./alist_data:/opt/alist/data
    environment:
    - PUID=0
    - PGID=0
    - UMASK=022
    restart: unless-stopped
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:5244'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_alist_port_http" model="service.template.port">
        <field name="template_id" ref="template_alist"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">5244</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_alist_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_alist"/>
        <field name="name">alist-data</field>
        <field name="local_path">./alist_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
