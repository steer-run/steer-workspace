<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Multi-servicio (además de 'searxng': valkey): revisar backup/red/init.
           - Owner UID:GID de 'valkey' (imagen valkey/valkey:8-alpine) desconocido → DEFAULT 1000:1000, verificar.
           - Owner UID:GID de 'searxng' (imagen searxng/searxng:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
           - Tiene 1 mount(s) de config: revisar rutas y montaje en el compose.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_searxng" model="service.template">
        <field name="name">SearXNG</field>
        <field name="code">searxng</field>
        <field name="service_code">searxng</field>
        <field name="version">latest</field>
        <field name="description">SearXNG is a privacy-respecting, self-hostable metasearch engine that aggregates results from hundreds of search services without tracking or profiling its users.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">SearXNG is a privacy-respecting, self-hostable metasearch engine that aggregates results from hundreds of search services without tracking or profiling its users.</p>
  <p>It forwards your queries to many upstream engines (Google, Bing, DuckDuckGo, Wikipedia and 200+ others), merges and de-duplicates the results, and returns them through your own instance so no single provider can build a profile of you. As the actively maintained successor to the original searx project, it follows a rolling-release model with fast engine fixes. It is also widely deployed as a private, programmatic search backend for AI agents and LLM pipelines.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Aggregates 200+ search engines with no user tracking, profiling or query logging</li>
    <li>"Bang" shortcuts to target a specific engine, plus per-engine and category filtering</li>
    <li>JSON output and an HTTP API for use as a search backend by scripts, agents and LLMs</li>
    <li>Highly configurable: themes, results proxying, autocompletion and per-engine settings</li>
    <li>Built-in rate limiting and bot detection to harden public-facing instances</li>
    <li>Works behind a reverse proxy and can be routed over Tor for additional anonymity</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Privacy-conscious individuals and teams who want a personal search portal free of tracking, and developers who need a self-hosted, API-driven search source for AI assistants, research tools and automation pipelines.</p>
</div>]]></field>
        <field name="category">productivity</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/searxng.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_search'), ref('agz_infra_suite.tag_privacy'), ref('agz_infra_suite.tag_security')])]"/>
        <field name="website_url">https://searxng.github.io/</field>
        <field name="documentation_url">https://docs.searxng.github.io/</field>
        <field name="source_url">https://github.com/searxng/searxng</field>
        <field name="docker_compose_template"><![CDATA[services:
  valkey:
    image: valkey/valkey:8-alpine
    command: valkey-server --save 30 1 --loglevel warning
    restart: unless-stopped
    volumes:
    - ./valkey_data:/data
    container_name: '{{ INSTANCE_CODE }}_valkey'
    networks:
    - internal
  searxng:
    image: searxng/searxng:latest
    restart: unless-stopped
    volumes:
    - ../files/searxng:/etc/searxng
    - ./searxng_data:/var/cache/searxng
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:8080'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_searxng_port_http" model="service.template.port">
        <field name="template_id" ref="template_searxng"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8080</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_searxng_var_secret_key" model="service.template.variable">
        <field name="template_id" ref="template_searxng"/>
        <field name="name">secret_key</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_searxng_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_searxng"/>
        <field name="name">valkey-data</field>
        <field name="local_path">./valkey_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_searxng_vol_1" model="service.template.volume">
        <field name="template_id" ref="template_searxng"/>
        <field name="name">searxng-data</field>
        <field name="local_path">./searxng_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_searxng_cfg_0" model="service.template.config.file">
        <field name="template_id" ref="template_searxng"/>
        <field name="name">settings.yml</field>
        <field name="filename">settings.yml</field>
        <field name="path">./files/searxng</field>
        <field name="content_template"><![CDATA[use_default_settings: true

server:
  secret_key: "${secret_key}"
  limiter: false
  image_proxy: false

valkey:
  url: valkey://valkey:6379/0
]]></field>
    </record>
</odoo>
