<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_gitingest" model="service.template">
        <field name="name">Gitingest</field>
        <field name="code">gitingest</field>
        <field name="service_code">gitingest</field>
        <field name="version">latest</field>
        <field name="description">Gitingest turns any Git repository into a single, prompt-friendly text digest that large language models can actually read.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Gitingest turns any Git repository into a single, prompt-friendly text digest that large language models can actually read.</p>
  <p>It flattens a codebase into one clean UTF-8 text blob along with useful statistics (file structure, size and approximate token count), so you can feed an entire project to an LLM instead of copy-pasting files one by one. Use it on the web by replacing "hub" with "ingest" in a GitHub URL, as a CLI tool, or as a Python library with a sync/async API. Built on FastAPI and fully self-hostable via Docker, it is aimed at developers and AI engineers who need to give models real codebase context.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Converts a repository into a single LLM-ready text digest with directory tree and token counting</li>
    <li>Three usage modes: web service, CLI (pip install gitingest), and Python library (sync/async)</li>
    <li>Targets subdirectories, branches and tags, and supports private repos via a GitHub personal access token</li>
    <li>Respects .gitignore rules and can include Git submodules</li>
    <li>Privacy-conscious: cloned repos are processed in memory and discarded, nothing is stored by default</li>
    <li>Self-hostable with Docker and Docker Compose, with optional Prometheus metrics, Sentry and S3-compatible storage for production deployments</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Developers, AI engineers and teams who want to quickly give LLMs (or AI coding agents) full codebase context for analysis, documentation or review, with a private self-hosted instance for internal and proprietary repositories.</p>
</div>]]></field>
        <field name="category">dev_tools</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/gitingest.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_dev_tools'), ref('agz_infra_suite.tag_ai'), ref('agz_infra_suite.tag_git')])]"/>
        <field name="website_url">https://gitingest.com</field>
        <field name="documentation_url">https://github.com/coderamp-labs/gitingest#readme</field>
        <field name="source_url">https://github.com/coderamp-labs/gitingest</field>
        <field name="docker_compose_template"><![CDATA[services:
  gitingest:
    image: elestio/gitingest:{{ software_version_tag }}
    restart: unless-stopped
    expose:
    - 8000
    environment:
    - ALLOWED_HOSTS={{ allowed_hosts }}
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:8000'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_gitingest_port_http" model="service.template.port">
        <field name="template_id" ref="template_gitingest"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_gitingest_var_software_version_tag" model="service.template.variable">
        <field name="template_id" ref="template_gitingest"/>
        <field name="name">software_version_tag</field>
        <field name="var_type">string</field>
        <field name="default_value">latest</field>
        <field name="sequence">10</field>
    </record>
</odoo>
