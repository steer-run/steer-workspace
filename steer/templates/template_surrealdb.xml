<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'surrealdb' (imagen surrealdb/surrealdb:v2.3.10) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_surrealdb" model="service.template">
        <field name="name">SurrealDB</field>
        <field name="code">surrealdb</field>
        <field name="service_code">surrealdb</field>
        <field name="version">2.3.10</field>
        <field name="description">SurrealDB is a multi-model database built in Rust that unifies documents, graphs, relations, time-series, vectors and full-text search behind a single query language.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">SurrealDB is a multi-model database built in Rust that unifies documents, graphs, relations, time-series, vectors and full-text search behind a single query language.</p>
  <p>It collapses what would normally require several specialized data stores into one engine, queried through SurrealQL and reachable over HTTP, WebSocket and native SDKs. SurrealDB can run as a standalone server, embedded directly in an application, or distributed across a cluster, and it offers real-time live queries, granular record-level access control and built-in authentication. It targets developers who want relational, document and graph workloads, plus vector search for AI, without stitching together multiple systems.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Multi-model in one engine: document, graph, relational, time-series, geospatial and vector data</li>
    <li>SurrealQL query language with graph traversal, joins and schemaful or schemaless tables</li>
    <li>Real-time live queries that push changes to clients over WebSocket</li>
    <li>Built-in authentication, permissions and record-level access control</li>
    <li>Flexible deployment: standalone server, embedded library or distributed cluster</li>
    <li>Vector search and indexing for AI and retrieval workloads, with HTTP and SDK access</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Developers and teams building real-time, AI-enabled or graph-heavy applications who want to consolidate multiple data models into a single self-hosted backend. Note: the core engine is source-available under the Business Source License (self-hosting is allowed; reselling it as a managed database service is not).</p>
</div>]]></field>
        <field name="category">database</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/surrealdb.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_database'), ref('agz_infra_suite.tag_dev_tools'), ref('agz_infra_suite.tag_search'), ref('agz_infra_suite.tag_ai')])]"/>
        <field name="website_url">https://surrealdb.com</field>
        <field name="documentation_url">https://surrealdb.com/docs/surrealdb</field>
        <field name="source_url">https://github.com/surrealdb/surrealdb</field>
        <field name="docker_compose_template"><![CDATA[services:
  surrealdb:
    image: surrealdb/surrealdb:v2.3.10
    environment:
      SURREAL_USER: {{ surreal_user }}
      SURREAL_PASS: {{ surreal_pass }}
    volumes:
    - ./db_data:/usr/app/data
    user: root
    command: start rocksdb:/db_data/data.db
    pull_policy: always
    container_name: '{{ INSTANCE_CODE }}'
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:8000'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_surrealdb_port_http" model="service.template.port">
        <field name="template_id" ref="template_surrealdb"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_surrealdb_var_surrealdb_password" model="service.template.variable">
        <field name="template_id" ref="template_surrealdb"/>
        <field name="name">surrealdb_password</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_surrealdb_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_surrealdb"/>
        <field name="name">db_data</field>
        <field name="local_path">./db_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
