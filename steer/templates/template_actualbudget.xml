<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'actualbudget' (imagen docker.io/actualbudget/actual-server:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_actualbudget" model="service.template">
        <field name="name">Actual Budget</field>
        <field name="code">actualbudget</field>
        <field name="service_code">actualbudget</field>
        <field name="version">latest</field>
        <field name="description">Actual Budget is a fast, local-first personal finance app that brings envelope budgeting to your own infrastructure without surrendering your financial data.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Actual Budget is a fast, local-first personal finance app that brings envelope budgeting to your own infrastructure without surrendering your financial data.</p>
  <p>Originally a commercial product, Actual Budget was open-sourced under the MIT license and has become a leading self-hosted alternative to YNAB. Your data lives in a local SQLite file by default and syncs across devices through a lightweight server you control, with optional end-to-end encryption so the sync server only ever stores opaque encrypted blobs. It targets privacy-conscious individuals and households who want modern budgeting without trusting a cloud-only commercial app.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Envelope budgeting where every unit of money you actually hold is assigned a job</li>
    <li>Local-first, offline-capable design with multi-device sync via a self-hosted server</li>
    <li>Optional end-to-end encryption so sync data is unreadable by the server</li>
    <li>Bank syncing through GoCardless (EU/UK) and SimpleFIN (US/Canada), plus QIF, OFX, QFX, CAMT.053 and CSV imports</li>
    <li>Scheduled transactions, budgeting rules, split transactions and automatic transfer linking</li>
    <li>Net worth, cash flow and fully custom reports, with a robust undo/redo system</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Individuals, families and privacy-focused users who want disciplined envelope budgeting across desktop, web and mobile while keeping full ownership of their financial data on self-hosted infrastructure.</p>
</div>]]></field>
        <field name="category">finance</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/actualbudget.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_finance'), ref('agz_infra_suite.tag_privacy'), ref('agz_infra_suite.tag_productivity')])]"/>
        <field name="website_url">https://actualbudget.org</field>
        <field name="documentation_url">https://actualbudget.org/docs</field>
        <field name="source_url">https://github.com/actualbudget/actual</field>
        <field name="docker_compose_template"><![CDATA[services:
  actualbudget:
    image: docker.io/actualbudget/actual-server:latest
    environment:
    - ACTUAL_PORT=5006
    volumes:
    - ./actual_data:/data
    restart: unless-stopped
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:5006'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_actualbudget_port_http" model="service.template.port">
        <field name="template_id" ref="template_actualbudget"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">5006</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_actualbudget_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_actualbudget"/>
        <field name="name">actual-data</field>
        <field name="local_path">./actual_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
