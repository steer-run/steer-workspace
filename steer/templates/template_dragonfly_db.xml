<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'dragonflydb' (imagen docker.dragonflydb.io/dragonflydb/dragonfly) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
           - Tiene 1 mount(s) de config: revisar rutas y montaje en el compose.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_dragonfly_db" model="service.template">
        <field name="name">Dragonfly</field>
        <field name="code">dragonfly_db</field>
        <field name="service_code">dragonfly_db</field>
        <field name="version">1.28.1</field>
        <field name="description">Dragonfly is a modern, multi-threaded in-memory data store that drops in for Redis and Memcached while scaling vertically to use all of a machine's cores.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Dragonfly is a modern, multi-threaded in-memory data store that drops in for Redis and Memcached while scaling vertically to use all of a machine's cores.</p>
  <p>Built from scratch in C++ on a shared-nothing architecture (not a Redis fork), Dragonfly keeps full Redis and Memcached API compatibility so existing clients and code work unchanged. It targets cache and data workloads that outgrow a single-threaded Redis process, delivering dramatically higher throughput and lower resource usage on modern multi-core hardware, without the operational overhead of running a cluster.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Drop-in Redis and Memcached protocol compatibility (no code changes to adopt)</li>
    <li>Multi-threaded, shared-nothing design that scales vertically across all CPU cores</li>
    <li>Novel unified cache eviction algorithm with low memory overhead</li>
    <li>Fork-free, point-in-time snapshots for fast and consistent persistence</li>
    <li>Built-in HTTP console and native Prometheus metrics for observability</li>
    <li>Configurable memory limits and cache eviction policies</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams running heavy caching or in-memory data workloads that hit the ceiling of single-threaded Redis, and those who want to scale up on a single large node instead of sharding across a cluster. A strong fit for self-hosted caching, session stores, queues and real-time data layers on modern cloud hardware.</p>
</div>]]></field>
        <field name="category">database</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/dragonfly_db.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_database'), ref('agz_infra_suite.tag_cache')])]"/>
        <field name="website_url">https://www.dragonflydb.io/</field>
        <field name="documentation_url">https://www.dragonflydb.io/docs</field>
        <field name="source_url">https://github.com/dragonflydb/dragonfly</field>
        <field name="docker_compose_template"><![CDATA[services:
  dragonflydb:
    image: docker.dragonflydb.io/dragonflydb/dragonfly
    ulimits:
      memlock: -1
    ports:
    - '{{ PORT_HTTP }}:6379'
    volumes:
    - ./dragonflydata:/data
    environment:
    - DFLY_requirepass
    container_name: '{{ INSTANCE_CODE }}'
    restart: unless-stopped
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_dragonfly_db_port_http" model="service.template.port">
        <field name="template_id" ref="template_dragonfly_db"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">6379</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_dragonfly_db_var_db_password" model="service.template.variable">
        <field name="template_id" ref="template_dragonfly_db"/>
        <field name="name">db_password</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_dragonfly_db_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_dragonfly_db"/>
        <field name="name">dragonflydata</field>
        <field name="local_path">./dragonflydata</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_dragonfly_db_cfg_0" model="service.template.config.file">
        <field name="template_id" ref="template_dragonfly_db"/>
        <field name="name">configuration.conf</field>
        <field name="filename">configuration.conf</field>
        <field name="path">./files/content</field>
        <field name="content_template"><![CDATA[bind-address = "0.0.0.0"
port = 6379
log-level = "info"
]]></field>
    </record>
</odoo>
