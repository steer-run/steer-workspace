<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_cloudflared" model="service.template">
        <field name="name">Cloudflared</field>
        <field name="code">cloudflared</field>
        <field name="service_code">cloudflared</field>
        <field name="version">latest</field>
        <field name="description">Cloudflared is Cloudflare's official Tunnel client, a lightweight daemon that publishes local services to the internet through a secure outbound-only connection, with no open inbound ports.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Cloudflared is Cloudflare's official Tunnel client, a lightweight daemon that publishes local services to the internet through a secure outbound-only connection, with no open inbound ports.</p>
  <p>Instead of forwarding ports or exposing a public IP, cloudflared runs next to your service and dials out to Cloudflare's network, which then routes inbound requests back through the encrypted tunnel. This hides your origin IP, keeps the firewall fully closed, and works even behind CGNAT. Written in Go and distributed as a single binary, Docker image, or system package, it plugs directly into Cloudflare Zero Trust so you can layer SSO, WAF, and access policies in front of any service.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Outbound-only tunnel with mutual TLS, post-quantum encryption, and no inbound firewall rules</li>
    <li>Exposes HTTP/HTTPS services and raw TCP (SSH, RDP) without port forwarding or a public IP</li>
    <li>Works behind CGNAT and dynamic IPs; automatic DNS (CNAME) record creation</li>
    <li>Free, automatically managed TLS certificates terminated at Cloudflare's edge</li>
    <li>Native Cloudflare Zero Trust integration: Access (SSO, OTP, hardware keys), WAF, and private-subnet routing</li>
    <li>Remotely-managed tunnels (token-based config in the dashboard) plus binaries, Docker, and Debian/RPM/Homebrew packages</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Self-hosters and teams that need to securely reach home or private services (dashboards, admin panels, internal tools, dev environments, webhooks) from anywhere without opening ports, plus operators who want Cloudflare's Zero Trust authentication and WAF in front of their apps. Best suited to HTTP/HTTPS workloads; note that public UDP is not supported and high-bandwidth media streaming may conflict with Cloudflare's terms.</p>
</div>]]></field>
        <field name="category">networking</field>
        <field name="is_web_app">False</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/cloudflared.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_networking'), ref('agz_infra_suite.tag_proxy'), ref('agz_infra_suite.tag_security'), ref('agz_infra_suite.tag_dns')])]"/>
        <field name="website_url">https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/</field>
        <field name="documentation_url">https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/get-started/</field>
        <field name="source_url">https://github.com/cloudflare/cloudflared</field>
        <field name="docker_compose_template"><![CDATA[services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    environment:
    - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN:?}
    network_mode: host
    restart: unless-stopped
    command:
    - tunnel
    - --no-autoupdate
    - run
    container_name: '{{ INSTANCE_CODE }}_cloudflared'
    networks:
    - internal
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_cloudflared_port_http" model="service.template.port">
        <field name="template_id" ref="template_cloudflared"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">80</field>
        <field name="traefik_routed">False</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_cloudflared_var_cloudflare_tunnel_token" model="service.template.variable">
        <field name="template_id" ref="template_cloudflared"/>
        <field name="name">CLOUDFLARE_TUNNEL_TOKEN</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
</odoo>
