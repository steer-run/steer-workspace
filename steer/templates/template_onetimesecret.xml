<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_onetimesecret" model="service.template">
        <field name="name">One Time Secret</field>
        <field name="code">onetimesecret</field>
        <field name="service_code">onetimesecret</field>
        <field name="version">latest</field>
        <field name="description">One Time Secret lets you share passwords and sensitive data through single-use links that self-destruct the moment they are read.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">One Time Secret lets you share passwords and sensitive data through single-use links that self-destruct the moment they are read.</p>
  <p>Instead of leaving credentials sitting in email threads or chat logs, you paste a secret and get back a unique URL that works exactly once: after the recipient opens it, the secret is permanently destroyed. It is the recognized open-source standard for one-time secret sharing, used by IT teams, support desks and security-conscious organizations that need a controlled way to hand off confidential information.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Single-use, self-destructing links that delete the secret after one view</li>
    <li>Configurable expiration so unread secrets vanish automatically</li>
    <li>Optional per-secret passphrase for an extra protection layer</li>
    <li>Server-side encryption of stored secrets, on an open MIT-licensed codebase</li>
    <li>Simple mode (Redis/Valkey only) or Full mode with accounts, MFA, SSO and WebAuthn</li>
    <li>Custom domains, REST API and support for 15+ interface languages</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams and individuals who need to hand off passwords, API keys or confidential notes without leaving copies in inboxes or chat history, and who want full control by self-hosting the service.</p>
</div>]]></field>
        <field name="category">security</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/onetimesecret.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_security'), ref('agz_infra_suite.tag_privacy'), ref('agz_infra_suite.tag_communication')])]"/>
        <field name="website_url">https://onetimesecret.com</field>
        <field name="documentation_url">https://docs.onetimesecret.com</field>
        <field name="source_url">https://github.com/onetimesecret/onetimesecret</field>
        <field name="docker_compose_template"><![CDATA[services:
  onetimesecret-redis:
    image: redis:7-alpine
    command: redis-server --requirepass {{ redis_password }}
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 10s
      retries: 3
    container_name: '{{ INSTANCE_CODE }}_onetimesecret_redis'
    networks:
    - internal
  onetimesecret:
    image: onetimesecret/onetimesecret:latest
    restart: unless-stopped
    environment:
    - AUTH_AUTOVERIFY=true
    - AUTH_SIGNUP=true
    - COLONEL={{ colonel }}
    - HOST={{ host }}
    - RACK_ENV=production
    - REDIS_URL=redis://:{{ redis_password }}@onetimesecret-redis:6379/0
    - SECRET={{ secret }}
    - SSL=true
    depends_on:
    - onetimesecret-redis
    healthcheck:
      test:
      - CMD
      - ruby
      - -rnet/http
      - -e
      - 'exit(Net::HTTP.get_response(URI(''http://localhost:3000'')).is_a?(Net::HTTPSuccess) ? 0 : 1)'
      interval: 30s
      timeout: 10s
      retries: 3
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:3000'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_onetimesecret_port_http" model="service.template.port">
        <field name="template_id" ref="template_onetimesecret"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">3000</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_onetimesecret_var_secret" model="service.template.variable">
        <field name="template_id" ref="template_onetimesecret"/>
        <field name="name">secret</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_onetimesecret_var_redis_password" model="service.template.variable">
        <field name="template_id" ref="template_onetimesecret"/>
        <field name="name">redis_password</field>
        <field name="var_type">password</field>
        <field name="is_secret">True</field>
        <field name="sequence">20</field>
    </record>
    <record id="template_onetimesecret_var_colonel" model="service.template.variable">
        <field name="template_id" ref="template_onetimesecret"/>
        <field name="name">colonel</field>
        <field name="var_type">string</field>
        <field name="default_value">admin@example.com</field>
        <field name="sequence">30</field>
    </record>
</odoo>
