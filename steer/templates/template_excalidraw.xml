<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_excalidraw" model="service.template">
        <field name="name">Excalidraw</field>
        <field name="code">excalidraw</field>
        <field name="service_code">excalidraw</field>
        <field name="version">latest</field>
        <field name="description">Excalidraw is an open-source virtual whiteboard for sketching diagrams and ideas with a distinctive hand-drawn look.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Excalidraw is an open-source virtual whiteboard for sketching diagrams and ideas with a distinctive hand-drawn look.</p>
  <p>It provides an infinite canvas where teams capture wireframes, architecture diagrams, flowcharts and quick sketches. The application is local-first: drawings are stored in the browser, so the self-hosted server simply serves the static app and never accesses your content. It is a leading open-source alternative to Miro, FigJam and Lucidchart, and is also embeddable as a component in other products.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Infinite canvas with a hand-drawn sketch style</li>
    <li>Local-first storage with offline (PWA) support</li>
    <li>Real-time collaboration with end-to-end encryption (via the Room server)</li>
    <li>Export to PNG, SVG, clipboard and the open .excalidraw JSON format</li>
    <li>Shape libraries, dark mode and an extensive component ecosystem</li>
    <li>Lightweight, privacy-friendly self-hosting with no analytics or tracking</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Teams and individuals who need fast visual thinking and diagramming: engineers documenting architecture, product teams running design sessions, educators, and anyone wanting a private, self-hosted alternative to commercial whiteboards.</p>
</div>]]></field>
        <field name="category">collaboration</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/excalidraw.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_collaboration'), ref('agz_infra_suite.tag_productivity'), ref('agz_infra_suite.tag_documentation'), ref('agz_infra_suite.tag_diagramming')])]"/>
        <field name="website_url">https://excalidraw.com/</field>
        <field name="documentation_url">https://docs.excalidraw.com/</field>
        <field name="source_url">https://github.com/excalidraw/excalidraw</field>
        <field name="docker_compose_template"><![CDATA[services:
  excalidraw:
    restart: unless-stopped
    image: excalidraw/excalidraw:latest
    healthcheck:
      test:
      - CMD
      - wget
      - --spider
      - --quiet
      - http://localhost
      interval: 30s
      timeout: 5s
      retries: 3
    container_name: '{{ INSTANCE_CODE }}'
    ports:
    - '{{ PORT_HTTP }}:80'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_excalidraw_port_http" model="service.template.port">
        <field name="template_id" ref="template_excalidraw"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">80</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
</odoo>
