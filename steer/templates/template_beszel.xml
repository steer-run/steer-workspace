<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'beszel' (imagen henrygd/beszel:0.10.2) desconocido → DEFAULT 1000:1000, verificar.
           - Sin servicio de DB: si la app usa SQLite, pasá backup_mode=custom con `sqlite3 .backup` y exclude_from_backup en el volumen del .db.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_beszel" model="service.template">
        <field name="name">Beszel</field>
        <field name="code">beszel</field>
        <field name="service_code">beszel</field>
        <field name="version">0.10.2</field>
        <field name="description">Beszel is a lightweight, self-hosted server monitoring hub that delivers historical metrics, Docker container stats, and configurable alerts from a single clean dashboard.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Beszel is a lightweight, self-hosted server monitoring hub that delivers historical metrics, Docker container stats, and configurable alerts from a single clean dashboard.</p>
  <p>Built on a hub-and-agent architecture, Beszel runs a central hub with a web dashboard while small single-binary agents collect metrics on each monitored machine. It is private by default and does not require exposing your systems to the internet, making it a practical alternative to heavier stacks like Prometheus plus Grafana for homelabs and small server fleets.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Host metrics: CPU, memory, disk, network, I/O, temperature, GPU, battery, and S.M.A.R.T. disk health</li>
    <li>Per-container Docker and Podman stats with historical CPU, memory, and network graphs</li>
    <li>Configurable alerts for CPU, memory, disk, bandwidth, temperature, and system status</li>
    <li>Single-binary agents with a minimal footprint and no external dependencies</li>
    <li>Multi-user access with OAuth2/OIDC authentication and optional password disabling</li>
    <li>Automatic backups to local disk or S3-compatible storage, plus a REST API for integrations</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Homelab users, sysadmins, and small teams who want fast, low-overhead visibility into a handful of servers and their containers without the complexity of a full observability stack.</p>
</div>]]></field>
        <field name="category">monitoring</field>
        <field name="is_web_app">True</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/beszel.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_monitoring'), ref('agz_infra_suite.tag_analytics'), ref('agz_infra_suite.tag_dev_tools')])]"/>
        <field name="website_url">https://beszel.dev</field>
        <field name="documentation_url">https://beszel.dev/guide/getting-started</field>
        <field name="source_url">https://github.com/henrygd/beszel</field>
        <field name="docker_compose_template"><![CDATA[services:
  beszel:
    image: henrygd/beszel:0.10.2
    restart: unless-stopped
    ports:
    - '{{ PORT_HTTP }}:8090'
    volumes:
    - ./beszel_data:/beszel_data
    - /var/run/docker.sock:/var/run/docker.sock:ro
    container_name: '{{ INSTANCE_CODE }}'
    networks:
    - internal
    - agz-proxy-network
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_beszel_port_http" model="service.template.port">
        <field name="template_id" ref="template_beszel"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">8090</field>
        <field name="traefik_routed">True</field>
        <field name="sequence">10</field>
    </record>
    <record id="template_beszel_vol_0" model="service.template.volume">
        <field name="template_id" ref="template_beszel"/>
        <field name="name">beszel_data</field>
        <field name="local_path">./beszel_data</field>
        <field name="owner">1000:1000</field>
        <field name="exclude_from_backup">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
