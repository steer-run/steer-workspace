<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ========================================================
         REVISAR antes de publicar (heurísticas inciertas):
           - Owner UID:GID de 'any-sync-bundle' (imagen ghcr.io/grishy/any-sync-bundle:latest) desconocido → DEFAULT 1000:1000, verificar.
           - Imagen en 'latest': fijá un tag estable (variable selection) antes de producción.
         Validá con el botón «Validar» y el procedimiento E2E de la guía §6.
    ======================================================== -->
    <record id="template_anytype" model="service.template">
        <field name="name">Anytype</field>
        <field name="code">anytype</field>
        <field name="service_code">anytype</field>
        <field name="version">latest</field>
        <field name="description">Anytype is a local-first, end-to-end encrypted knowledge base that works like your own private, self-hosted Notion.</field>
        <field name="rich_description"><![CDATA[<div class="agz-app-desc">
  <p class="agz-app-lead">Anytype is a local-first, end-to-end encrypted knowledge base that works like your own private, self-hosted Notion.</p>
  <p>Anytype lets you capture, connect and remix all kinds of information into pages, tasks, wikis, journals and custom databases, while defining your own object types and relations. Data lives offline-first on your devices and syncs through the encrypted any-sync protocol, so providers can never read your content. This catalog entry deploys the community any-sync bundle: an all-in-one backend that merges the official coordinator, consensus, sync and file nodes into a single service, letting you run a fully private sync network for your Anytype clients.</p>
  <h6 class="agz-app-subhead">Key features</h6>
  <ul class="agz-app-feats">
    <li>Local-first storage with end-to-end, zero-knowledge encryption across desktop and mobile</li>
    <li>Composable blocks and objects: text, tasks, kanban, calendar, relations and custom data models</li>
    <li>Self-hosted sync server so your data stays on infrastructure you control</li>
    <li>All-in-one backend bundling coordinator, consensus, sync and file nodes in one deployment</li>
    <li>Flexible storage with embedded BadgerDB by default or S3-compatible backends (MinIO, R2, B2, S3)</li>
    <li>Lightweight footprint suitable for homelabs and small servers, exposing only two ports</li>
  </ul>
  <h6 class="agz-app-subhead">Best for</h6>
  <p>Privacy-conscious individuals and teams who want a Notion-style workspace for notes, wikis and knowledge management without trusting a third-party cloud, and self-hosters who want full control over where their encrypted data is synced and stored.</p>
</div>]]></field>
        <field name="category">knowledge</field>
        <field name="is_web_app">False</field>
        <field name="requires_database">False</field>
        <field name="requires_backup">True</field>
        <field name="backup_mode">standard</field>
        <field name="image" type="base64" file="agz_infra_templates/static/img/templates/anytype.png"/>
        <field name="tag_ids" eval="[Command.set([ref('agz_infra_suite.tag_self_hosted'), ref('agz_infra_suite.tag_notes'), ref('agz_infra_suite.tag_documentation'), ref('agz_infra_suite.tag_productivity'), ref('agz_infra_suite.tag_collaboration'), ref('agz_infra_suite.tag_privacy')])]"/>
        <field name="website_url">https://anytype.io/</field>
        <field name="documentation_url">https://doc.anytype.io/anytype-docs</field>
        <field name="source_url">https://github.com/grishy/any-sync-bundle</field>
        <field name="docker_compose_template"><![CDATA[services:
  any-sync-bundle:
    image: ghcr.io/grishy/any-sync-bundle:latest
    restart: unless-stopped
    ports:
    - 33010:33010
    - 33020:33020/udp
    volumes:
    - ./data:/data
    environment:
      ANY_SYNC_BUNDLE_INIT_EXTERNAL_ADDRS: 192.168.100.9
    container_name: '{{ INSTANCE_CODE }}_any_sync_bundle'
    networks:
    - internal
networks:
  internal:
    driver: bridge
  agz-proxy-network:
    external: true
]]></field>
    </record>
    <record id="template_anytype_port_http" model="service.template.port">
        <field name="template_id" ref="template_anytype"/>
        <field name="code">http</field>
        <field name="name">HTTP</field>
        <field name="default_port">80</field>
        <field name="traefik_routed">False</field>
        <field name="sequence">10</field>
    </record>
</odoo>
